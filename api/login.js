const {sign,cookieHeader,json}=require('./_lib');
module.exports=async function(req,res){if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});try{const body=typeof req.body==='string'?JSON.parse(req.body||'{}'):req.body||{};const user=process.env.PORTAL_USER||'jake',pass=process.env.PORTAL_PASSWORD;if(!pass)return json(res,500,{error:'Portal password is not configured.'});if(body.username!==user||body.password!==pass)return json(res,401,{error:'Invalid username or password.'});const token=sign({sub:user,exp:Date.now()+8*3600000});return json(res,200,{ok:true},{'Set-Cookie':cookieHeader(token)})}catch(e){return json(res,500,{error:e.message})}}
